SELECT 
    id,
    agrupamento_id,
    numped,
    rota,
    DATE_SUB(created_at, INTERVAL 3 HOUR) as created_at_br,
    filial
FROM pedidos_nao_atrelados
WHERE DATE(DATE_SUB(created_at, INTERVAL 3 HOUR)) = CURDATE()
ORDER BY created_at ASC